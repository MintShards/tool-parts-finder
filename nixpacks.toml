[phases.setup]
nixPkgs = ["python311"]

[phases.install]
cmds = ["cd backend && pip install --user -r requirements.txt"]

[start]
cmd = "cd backend && uvicorn app.main:app --host 0.0.0.0 --port $PORT"
